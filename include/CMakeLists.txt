ADD_SUBDIRECTORY(cuda)

FILE(GLOB_RECURSE HFiles "${CMAKE_SOURCE_DIR}/include/*.h")
FILE(GLOB_RECURSE HXXFiles "${CMAKE_SOURCE_DIR}/include/*.hxx")

INSTALL(FILES 
        ${HFiles}
        ${HXXFiles}
DESTINATION
	${SKM_INCLUDE_DESTINATION}
PERMISSIONS
	OWNER_READ GROUP_READ WORLD_READ
)